<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shopinfo">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditCustomerActivity"></activity>
        <activity android:name=".EditSelllerActivity" />
        <activity android:name=".DetailsActivity" />
        <activity android:name=".Share" />
        <activity android:name=".About" />
        <activity android:name=".SellerActivity" />
        <activity android:name=".SellerRegestration" />
        <activity android:name=".CustomerRegestration" />
        <activity android:name=".Signup" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>